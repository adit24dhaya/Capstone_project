apiVersion: v1
kind: Pod
metadata:
  name: pcb-rt640-artifact-reader
  namespace: csuf-titans
spec:
  restartPolicy: Never
  containers:
    - name: reader
      image: alpine:3.20
      command: ["/bin/sh", "-c", "sleep 86400"]
      resources:
        requests:
          cpu: "100m"
          memory: 128Mi
        limits:
          cpu: "500m"
          memory: 512Mi
      volumeMounts:
        - name: workspace
          mountPath: /workspace
          readOnly: true
  volumes:
    - name: workspace
      persistentVolumeClaim:
        claimName: pcb-rtdetr-640-workspace
